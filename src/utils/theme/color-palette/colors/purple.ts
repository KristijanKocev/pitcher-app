import { generateSolidColors, generateTintColors } from '../utils';

export const purpleSolids = generateSolidColors(300, 100);
export const purpleTintsLight = generateTintColors(300, 100, 30);
export const purpleTintsDark = generateTintColors(300, 100, 80);
