import { generateSolidColors, generateTintColors } from '../utils';

export const greenSolids = generateSolidColors(120, 100);
export const greenTintsLight = generateTintColors(120, 100, 35);
export const greenTintsDark = generateTintColors(120, 100, 85);
