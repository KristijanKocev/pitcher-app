import { generateSolidColors, generateTintColors } from '../utils';

export const blueSolids = generateSolidColors(240, 100);
export const blueTintsLight = generateTintColors(240, 100, 35);
export const blueTintsDark = generateTintColors(240, 100, 85);
