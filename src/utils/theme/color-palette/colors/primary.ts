import { generateSolidColors, generateTintColors } from '../utils';

export const primarySolids = generateSolidColors(0, 0);
export const primaryTintsLight = generateTintColors(0, 0, 15);
export const primaryTintsDark = generateTintColors(0, 0, 100);
