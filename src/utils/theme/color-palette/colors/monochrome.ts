import { generateSolidColors, generateTintColors } from '../utils';

export const monochromeSolids = generateSolidColors(0, 0);
export const monochromeTintsLight = generateTintColors(0, 0, 15);
export const monochromeTintsDark = generateTintColors(0, 0, 100);
