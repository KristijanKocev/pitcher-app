import { generateSolidColors, generateTintColors } from '../utils';

export const redSolids = generateSolidColors(0, 100);
export const redTintsLight = generateTintColors(0, 100, 35);
export const redTintsDark = generateTintColors(0, 100, 85);
