import { generateSolidColors, generateTintColors } from '../utils';

export const yellowSolids = generateSolidColors(60, 100);
export const yellowTintsLight = generateTintColors(60, 100, 40);
export const yellowTintsDark = generateTintColors(60, 100, 85);
